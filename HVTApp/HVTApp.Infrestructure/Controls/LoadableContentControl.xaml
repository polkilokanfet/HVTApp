<UserControl x:Class="HVTApp.Infrastructure.Controls.LoadableContentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">

    <UserControl.ContentTemplate>
        <DataTemplate>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel
                    x:Name="LoadElement"
                    Orientation="Vertical" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    Visibility="{Binding LoadingInProcess, Converter={StaticResource BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged, Mode=OneWay, ElementName=Root}">

                    <Label Content="Загрузка данных..." HorizontalAlignment="Center"/>
                    <ProgressBar x:Name="ProgressBar1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="300" Height="15" Margin="5, 10" IsIndeterminate="True" />
                </StackPanel>

                <ContentPresenter x:Name="Content1" 
                                  Content="{TemplateBinding Content}">
                    <ContentPresenter.Style>
                        <Style>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=LoadElement, Path=Visibility}" Value="Visible">
                                    <Setter Property="Label.Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentPresenter.Style>

                </ContentPresenter>
            </Grid>
        </DataTemplate>
    </UserControl.ContentTemplate>

</UserControl>
