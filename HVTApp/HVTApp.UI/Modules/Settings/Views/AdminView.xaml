<inf:ViewBase x:Class="HVTApp.UI.Modules.Settings.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:inf="clr-namespace:HVTApp.Infrastructure;assembly=HVTApp.Infrastructure"
             prism:ViewModelLocator.AutoWireViewModel="True">
    
    <StackPanel Orientation="Vertical">
        <Button Content="Command1" Command="{Binding Command1}"/>
        <Button Content="Command2" Command="{Binding Command2}"/>
        <Button Content="Выбрать продукт" Command="{Binding Command3}"/>
        <Button Content="Выбрать блок" Command="{Binding Command4}"/>
        <Button Content="Выбрать блок (добавленные продукты)" Command="{Binding Command5}"/>
    </StackPanel>
    
</inf:ViewBase>

