<inf:ViewBase x:Class="HVTApp.UI.Modules.Settings.Views.UserSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:inf="clr-namespace:HVTApp.Infrastructure;assembly=HVTApp.Infrastructure"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:viewModels="clr-namespace:HVTApp.UI.Modules.Settings.ViewModels"
             d:DataContext="{d:DesignInstance viewModels:UserSettingsViewModel, IsDesignTimeCreatable=True}">

    <Border Background="White" Padding="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <GroupBox Grid.Row="0" Header="Настройки">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <CheckBox Grid.Row="0" Grid.Column="0" IsChecked="{Binding User.IsPriceEngineeringTaskMessagesEnabled}"/>
                    <Label Grid.Row="0" Grid.Column="1" Content=" - показывать уведомления о сообщениях в задачах ТСП"/>

                    <Button Grid.Row="1" Grid.Column="1" Content="Сохранить" Command="{Binding SaveCommand}"/>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="1" Header="Изменение пароля">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="400"/>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                
                    <Label Grid.Row="0" Grid.Column="0" Content="Старый пароль: "/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PasswordViewModel.PassOld, UpdateSourceTrigger=PropertyChanged}"/>
                
                    <Label Grid.Row="1" Grid.Column="0" Content="Новый пароль: " />
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PasswordViewModel.PassNew, UpdateSourceTrigger=PropertyChanged}"/>
                
                    <Label Grid.Row="2" Grid.Column="0" Content="Новый пароль (повторно): " />
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding PasswordViewModel.PassAgain, UpdateSourceTrigger=PropertyChanged}"/>
                
                    <Button Grid.Row="3" Grid.Column="1" Content="Ok" Command="{Binding PasswordViewModel.OkCommand}"/>

                </Grid>
            </GroupBox>
        </Grid>
    </Border>

</inf:ViewBase>